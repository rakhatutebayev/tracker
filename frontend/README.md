# GPS Трекер - React + Vite Frontend

Реал-тайм визуализация GPS позиций машин на карте и подробные отчеты по маршрутам.

## Стек

- **React 18** + **Vite** (быстрая разработка)
- **Ant Design 5** (UI компоненты, русская локализация)
- **Leaflet** + **React Leaflet** (интерактивная карта)
- **Zustand** (state management)
- **Axios** (HTTP клиент)
- **Day.js** (работа с датами)

## Структура

```
src/
├── components/
│   ├── LiveMap.jsx          # Реал-тайм карта с маркерами машин
│   ├── TripsReport.jsx      # Таблица поездок + экспорт CSV
│   └── SummaryReport.jsx    # Сводка по месяцу/неделе
├── pages/
│   └── MainLayout.jsx       # Главное окно (меню + контент)
├── hooks/
│   └── useWebSocket.js      # Hook для WebSocket подписки
├── services/
│   └── api.js               # HTTP клиент для API
├── store/
│   └── trackingStore.js     # Zustand store
├── App.jsx
└── main.jsx
```

## Функциональность

### 1️⃣ Реал-тайм карта

- **Живые позиции** машин на карте (обновляется через WebSocket)
- **Маркеры** с инфо: название, координаты, скорость, время
- **2GIS Tiles** для Казахстана (используется OpenStreetMap)
- **Автоцентрирование** на первую выбранную машину

### 2️⃣ Отчет по маршрутам

Таблица всех поездок (trip) с фильтром по машине и дате:

| Колонка | Описание |
|---------|----------|
| **Пункт выезда** | Координаты старта |
| **Дата/Время выезда** | Когда машина начала ехать |
| **Пункт приезда** | Координаты финиша |
| **Дата/Время приезда** | Когда машина остановилась |
| **Время в пути** | Длительность в часах |
| **Расстояние** | Путь в км (Haversine) |
| **Средняя скорость** | км/ч |
| **Макс скорость** | км/ч |

**Экспорт**: CSV кнопка для сохранения данных в Excel

### 3️⃣ Итоговый отчет

Сводка по периоду (месяц/неделя):
- 📊 Количество поездок
- 📏 Общее расстояние
- ⏱️ Общее время в пути
- 🚗 Средняя/макс скорость

## Установка

```bash
cd frontend
npm install
npm run dev
```

Откроется на http://localhost:3000

## Build для production

```bash
npm run build
npm run preview
```

## Интеграция с бэкендом

Frontend проксирует запросы к `localhost:8000`:

- `GET /api/devices` — список машин
- `GET /api/positions/latest` — текущие позиции
- `POST /api/reports/trips` — отчет по поездкам
- `GET /api/reports/summary` — сводка
- `ws://localhost:8000/ws/tracker?devices=1,2,3` — WebSocket live updates

## Локализация

Интерфейс полностью на **русском языке**:
- Ant Design русская локаль
- Dayjsru
- Все кнопки, меню, таблицы на русском

## 2GIS Карта

Пока используется **OpenStreetMap** (бесплатно). Для интеграции с 2GIS API:

1. Получить API key на https://2gis.kg/
2. Обновить `LiveMap.jsx`:

```jsx
<TileLayer
  url="https://tile.2gis.com/tiles?x={x}&y={y}&z={z}&v=1"
  attribution='© 2GIS'
/>
```

3. Добавить параметр API key в URL (если требуется)

## TODO

- [ ] Интеграция с реальным 2GIS API
- [ ] Полный плейбэк маршрутов (timeline scrubber)
- [ ] Поиск по адресу (геокодирование Яндекс)
- [ ] Уведомления (превышение скорости, выезд из геозоны)
- [ ] Импорт из Traccar старой БД
- [ ] Multi-user + authentication
- [ ] Dark mode
